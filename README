USAGE:

CoffeeScript:
=============
event = new jsEvents.Event

observer1 = event.register (name) ->
   alert name
observer2 = event.register (name) ->
   alert name + "Smith"

event.notify "Craig"

event.unregister observer1
event.notify "Another"


Javascript:
===========
event = new jsEvents.Event;

observer1 = event.register(function(name) {
  alert(name);
});
observer2 = event.register(function(name) {
  alert(name + "Smith");
});

event.notify("Craig");

event.unregister(observer1);
event.notify("Another");